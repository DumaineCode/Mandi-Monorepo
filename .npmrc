auto-install-peers=true
# Our Hetzner build IP is rate-limited (E429) by registry.npmjs.org AND by the
# Yarn proxy. Use registry.npmmirror.com (independent Alibaba mirror) which is
# not blocking this IP. Verified working from the build server.
registry=https://registry.npmmirror.com
