auto-install-peers=true
# Use the Yarn registry mirror (Cloudflare-backed, 1:1 sync with npmjs.org).
# Our Hetzner build IP gets rate-limited (E429) by registry.npmjs.org, so we
# point npm at a mirror that isn't blocking this IP range.
registry=https://registry.yarnpkg.com
